---
title: {{ domain }} domain
---

## {{ domain }} domain

### {{ domain }} Commands

{% for definition in domainDefinitions.command %}

#### {{ definition.simpleCommandClass }} {{ (definition.minver) ? '{{< minver v="%minver%" >}}'|replace({'%minver%': definition.minver})|raw : '' }} {id="{{ definition.simpleCommandClass }}"}

{{ '{{% ' }} include-md "{{ partialFolder }}/{{ definition.slugName }}.md" {{ '%}}' }}
{% endfor %}

### {{ domain }} Queries

{% for definition in domainDefinitions.query %}
#### {{ definition.simpleCommandClass }} {id="{{ definition.simpleCommandClass }}"}

{{ '{{% ' }} include-md "{{ partialFolder }}/{{ definition.slugName }}.md" {{ '%}}' }}
{% endfor %}
