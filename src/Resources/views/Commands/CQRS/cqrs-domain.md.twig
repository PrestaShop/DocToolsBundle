---
title: {{ domain }} domain
---

## {{ domain }} domain

### {{ domain }} Commands

{% for definition in domainDefinitions.command %}
{{ '{{% ' }} include-md "{{ partialFolder }}/{{ definition.slugName }}.md" {{ '%}}' }}
{% endfor %}

### {{ domain }} Queries

{% for definition in domainDefinitions.query %}
{{ '{{% ' }} include-md "{{ partialFolder }}/{{ definition.slugName }}.md" {{ '%}}' }}
{% endfor %}
